/* Without running the following code, what do you think it will output? */

function foo(bar, qux) {
    console.log(bar);
    console.log(qux);
  }
  
  foo('Hello');

  //Solution
  // I think it will log hello for bar, and undefined for qux 