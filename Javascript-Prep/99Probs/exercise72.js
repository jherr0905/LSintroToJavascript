/* Write a function that returns the 
last element of an input array. For example:*/

// last(['Earth', 'Moon', 'Mars']); // 'Mars'

//My Solution
function last(arr){
     return arr[arr.length - 1]
}

console.log(last(['Earth', 'Moon', 'Mars'])); // 'Mars'