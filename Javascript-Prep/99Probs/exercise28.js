/* Write a while loop that logs all odd natural numbers between 1 and 40.*/

/* while (condition)
  statement
*/

//Solution
let counter = 1;
while (counter <= 40) {
  console.log(counter);
  counter = counter + 2;
}