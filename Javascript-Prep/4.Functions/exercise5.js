// What does the following code log to the console?

function scream(words) {
    return words + '!!!!';
  }
  
  scream('Yipeee');

  // Solution
  // It doesn't console log anything because there isn't one
  // it returns Yipeee!!!! but not logs it